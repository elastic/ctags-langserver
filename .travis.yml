sudo: false
dist: trusty
branches:
  only:
  - master
  - "/^v\\d+\\.\\d+\\.\\d+(-alpha\\.\\d+)?$/"

compiler: clang
language: node_js

node_js:
- '10'

os:
- linux
- osx

install:
- set -e;
- yarn comile
- travis_retry yarn test